# Model Card

초기 repository는 모델 학습 전 단계입니다. `scripts/train_models.py`는 pair feature 로딩 smoke test만 제공합니다.

향후 DummyClassifier, LogisticRegression, RandomForestClassifier를 비교하되, 성능은 합성 규칙 기반 데이터셋의 내부 일관성 재현 성능으로만 설명합니다.
